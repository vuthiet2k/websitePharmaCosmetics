node_modules/
dist/
vercel-dist/
vercel-dist.zip
.env
.env.*
!.env.example

# Playwright (REOPEN 2026-09-11, Visual Regression Gate) — giữ lại baseline thật
# (tests/visual/__snapshots__/) và report evidence (.project-agent/evidence/visual/) vì cần
# commit cùng code làm bằng chứng E3; chỉ bỏ qua thư mục làm việc tạm thời của mỗi lần chạy.
test-results/
blob-report/
playwright/.cache/
