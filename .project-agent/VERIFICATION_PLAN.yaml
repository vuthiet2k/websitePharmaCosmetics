# VERIFICATION_PLAN.yaml — theo 29. Verification Execution Contract.
# Một gate chỉ PASSED khi lệnh trong COMMANDS.yaml đã chạy thật trên revision hiện tại.

gates:
  - id: GATE-BUILD
    profile: frontend_theme
    command_id: build_theme          # COMMANDS.yaml
    expected_exit_code: 0
    evidence_grade: E2
    applies_before: "Bất kỳ task M2-M4 nào coi là DONE"

  - id: GATE-VISUAL-QA
    profile: frontend_theme
    command_id: preview
    expected_result: "Render không lỗi console + đối chiếu thủ công với HTML tương ứng trong Pharma_Cosmetics_Figma_Project/"
    evidence_grade: E2
    applies_before: "AC-01..AC-18, AC-25 chuyển PASSED"

  - id: GATE-PII-FORM
    profile: frontend_theme
    check: "Mọi form trong SPEC.md nhóm B (booking/ai-quiz/ai-results) và patient-portal/loyalty/customer-account có consent checkbox + link chính sách"
    evidence_grade: E2
    applies_before: "AC-07, AC-09, AC-15, AC-17"

  - id: GATE-SECRET-SCAN
    profile: frontend_theme
    command_id: secret_scan
    expected_exit_code: 1
    evidence_grade: E2
    applies_before: "Bất kỳ commit nào chạm templates/snippets/assets"

  - id: GATE-VISUAL-REGRESSION
    profile: frontend_theme
    command_id: visual_regression_test          # COMMANDS.yaml, REOPEN 2026-09-11
    expected_exit_code: 0
    expected_result: "0 trang vượt ngưỡng maxDiffPixelRatio 0.0005 (0.05%) so với baseline tests/visual/__snapshots__/"
    evidence_grade: E3
    applies_before: "Bất kỳ thay đổi templates/snippets/layouts nào coi là DONE (thay thế 1 phần GATE-VISUAL-QA thủ công bằng pixel-diff tự động cho các trang đã có baseline)"
    note: "Lần đầu 1 trang chưa có baseline = thiết lập baseline (không tính PASSED regression). BLOCK_RELEASE nếu vượt ngưỡng và chưa xác nhận là thay đổi có chủ đích."

  - id: GATE-INTEGRATION-MOPS
    profile: mops_gas_backend
    command_id: gas_push_test
    expected_result: "Test qua bản /dev trước, verify payload thực từ GHTK/SPX/X-Signs, không chỉ HTTP 200"
    evidence_grade: E3
    applies_before: "AC-21, AC-22, AC-23"

  - id: GATE-RELEASE-MOPS
    profile: mops_gas_backend
    command_id: gas_deploy_production
    precondition: "GATE-INTEGRATION-MOPS PASSED + gas_deployments_list xác nhận đúng deploymentId"
    evidence_grade: E3
    applies_before: "Bất kỳ thay đổi mops-gas nào lên production"

execution_isolation_note: >
  Theo 11. Verification Separation — người/tiến trình thực hiện GATE-VISUAL-QA và
  GATE-INTEGRATION-MOPS không được là chính bước vừa sinh code đó trong cùng 1 lần khẳng định
  "xong". Phải chạy lệnh thật (COMMANDS.yaml) và đọc output, không tự suy luận "chắc là chạy
  được".
