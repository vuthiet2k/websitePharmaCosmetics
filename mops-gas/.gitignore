# clasp: KHÔNG bao giờ commit credential (nằm ở ~/.clasprc.json — ngoài repo sẵn).
# .clasp.json (chỉ chứa scriptId + rootDir, KHÔNG phải secret) → NÊN commit để cả nhóm chung scriptId.
# Nếu muốn giữ scriptId riêng tư thì bỏ comment dòng dưới:
# .clasp.json

.clasprc.json
node_modules/
*.log
